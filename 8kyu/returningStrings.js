// ❓ УСЛОВИЕ:

// Создайте функцию, которая принимает в качестве параметра имя и возвращает сообщение:  "Hello, <name> how are you doing today?".
// Убедитесь, что вы ввели именно то, что я написал, иначе программа может некорректно запуститься.

// ✅ РЕШЕНИE

function greet(name){
  return `Hello, ${name} how are you doing today?`
}